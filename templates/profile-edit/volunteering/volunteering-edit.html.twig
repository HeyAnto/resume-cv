{% extends 'base.html.twig' %}

{% block title %}Profile Cooking — Resume.cv{% endblock %}

{% block javascripts %}
{{ parent() }}
<script src="{{ asset('js/character-counter.js') }}"></script>
<script src="{{ asset('js/form-submit.js') }}"></script>
{% endblock %}

{% block main %}

{% include "_components/sidebar.html.twig" %}

{% include "/profile-edit/_components/edit-sidebar.html.twig" %}

<div class="main-content">
  {% include "_components/headers/header-logout.html.twig" %}

  <section class="section-profile-edit">
    <div class="container">

      <div>
        <h1 class="text-h1">{{ volunteering ? 'Edit Volunteering' : 'Create Volunteering' }}</h1>
        <p class="text-16 text-color-tertiary">{{ volunteering ? 'Update your volunteer experience' : 'Add your
          volunteer experience' }}</p>
      </div>

      {% for message in app.flashes('success') %}
      <div class="alert alert-success">
        {{ message }}
      </div>
      {% endfor %}

      {% for message in app.flashes('error') %}
      <div class="alert alert-danger">
        {{ message }}
      </div>
      {% endfor %}

      {{ form_start(form) }}

      <div class="form-group">
        <div class="flex justify-between items-center">
          <label class="text-16 text-color-tertiary" for="{{ form.organizationName.vars.id }}">{{
            form.organizationName.vars.label }}*</label>
          <p class="text-14 text-color-tertiary" id="organizationName-counter"></p>
        </div>
        {{ form_widget(form.organizationName) }}
        <div class="text-14 text-color-red">{{ form_errors(form.organizationName) }}</div>
      </div>

      <div class="form-group">
        <div class="flex justify-between items-center">
          <label class="text-16 text-color-tertiary" for="{{ form.organizationLink.vars.id }}">{{
            form.organizationLink.vars.label }}</label>
          <p class="text-14 text-color-tertiary" id="organizationLink-counter"></p>
        </div>
        {{ form_widget(form.organizationLink) }}
        <div class="text-14 text-color-red">{{ form_errors(form.organizationLink) }}</div>
      </div>

      <div class="form-group">
        <div class="flex justify-between items-center">
          <label class="text-16 text-color-tertiary" for="{{ form.role.vars.id }}">{{
            form.role.vars.label }}*</label>
          <p class="text-14 text-color-tertiary" id="role-counter"></p>
        </div>
        {{ form_widget(form.role) }}
        <div class="text-14 text-color-red">{{ form_errors(form.role) }}</div>
      </div>

      <div class="form-group">
        <div class="flex justify-between items-center">
          <label class="text-16 text-color-tertiary" for="{{ form.location.vars.id }}">{{
            form.location.vars.label }}*</label>
          <p class="text-14 text-color-tertiary" id="location-counter"></p>
        </div>
        {{ form_widget(form.location) }}
        <div class="text-14 text-color-red">{{ form_errors(form.location) }}</div>
      </div>

      <div class="form-group">
        <div class="flex justify-between items-center">
          <label class="text-16 text-color-tertiary" for="{{ form.description.vars.id }}">{{
            form.description.vars.label }}</label>
          <p class="text-14 text-color-tertiary" id="description-counter"></p>
        </div>
        {{ form_widget(form.description) }}
        <div class="text-14 text-color-red">{{ form_errors(form.description) }}</div>
      </div>

      <div class="form-group">
        <div class="flex justify-between items-center">
          <label class="text-16 text-color-tertiary" for="{{ form.startDate.vars.id }}">{{
            form.startDate.vars.label }}*</label>
        </div>
        {{ form_widget(form.startDate) }}
        <div class="text-14 text-color-red">{{ form_errors(form.startDate) }}</div>
      </div>

      <div class="form-group">
        <div class="flex justify-between items-center">
          <label class="text-16 text-color-tertiary" for="{{ form.endDate.vars.id }}">{{
            form.endDate.vars.label }}</label>
        </div>
        {{ form_widget(form.endDate) }}
        <div class="text-14 text-color-red">{{ form_errors(form.endDate) }}</div>
      </div>

      {{ form_widget(form.save, {'attr': {'class': 'btn btn-primary-xs full'}}) }}

      {{ form_end(form) }}

      {% if volunteering %}
      <form action="{{ path('app_volunteering_delete', {'username': username, 'id': volunteering.id}) }}" method="post"
        style="display: inline;"
        onsubmit="return confirm('Are you sure you want to delete this volunteering experience?')">
        <div class="flex justify-end">
          <button type="submit" class="btn btn-secondary-xs danger full">Delete</button>
        </div>
      </form>
      {% endif %}

    </div>
  </section>
</div>

{% endblock %}